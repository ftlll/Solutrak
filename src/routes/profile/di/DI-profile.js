import { DIBase } from './../../../resources/elements/DI-base';
import { AuthData } from './../../../models/auth-data';

export class DIProfile extends DIBase {

  constructor(m) {
    super(m);
  }

  //@todo: add here all computed | visual properties attached to an object having this type !!! do not add such properties in the main view controller !!!
}
